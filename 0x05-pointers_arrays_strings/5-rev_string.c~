#include "holberton.h"
/**
 * rev_string - function that reverses a string.
 * followed by a new line.
 * @s : char  type
 * Return: Always 0.
 */
void rev_string(char *s)
{
int i;
for (i = 0; s[i] != 0 ; i++)
{
}
i--;
for (i = 0; s[i] != 0 ; i--)
{
_putchar(s[i]);
}
}
