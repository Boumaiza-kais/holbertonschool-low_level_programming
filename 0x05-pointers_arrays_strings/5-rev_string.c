#include "holberton.h"
/**
 * rev_string - function that reverses a string.
 * followed by a new line.
 * @s : char  type
 * Return: Always 0.
 */
void rev_string(char *s)
{
int i;
int j;
int k; 
int len ;
len=0;
for (i = 0; s[len] != 0 ; i++)
{
len ++ ;  
}
 j=len -1;
for (i = 0; i < len /2 ; i++)
{
k;
}
}
