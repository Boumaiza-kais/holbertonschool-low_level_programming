#include"holberton.h"
/**
 * print_diagsums - prints sums of both diagonals of square matrix
 * @size: int type
 * @a: int type
 * Return: void
*/
void print_diagsums(int *a, int size)
{
int i = 0, j = 0, sum = 0;
while (i = j)
{
sum = a[i][j] + a[i + 1][j + 1]
i++;
j++
}
printf("%d\n", sum);
}
